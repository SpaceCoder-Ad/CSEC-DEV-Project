<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>400 - Invalid Credentials</title>
    <!-- <link rel="stylesheet" href="styles.css"> -->
    <style>
    /* General Body Styles */
    body {
        font-family: 'Arial', sans-serif;
        background-color: #121212;  /* Dark background */
        color: #fff;
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        text-align: center;
    }

    /* Container for centered content */
    .container {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100%;
    }

    /* Card Style for error message */
    .card {
        background-color: #1f1f1f;
        padding: 40px;
        border-radius: 15px;
        box-shadow: 0px 4px 20px rgba(0, 0, 0, 0.5);
        width: 350px;
        text-align: center;
    }

    /* Heading Style */
    .card h1 {
        font-size: 2.5em;
        margin-bottom: 20px;
        color: #ff4d4d;  /* Red color for error */
    }

    /* Text for the error message */
    .card p {
        font-size: 1.2em;
        color: #ccc;
        margin-bottom: 30px;
    }

    /* Error Details Preformatted Text */
    .error-details {
        background-color: #333;
        padding: 10px;
        border-radius: 5px;
        color: #ff9800;  /* Yellow color for error details */
        font-family: 'Courier New', monospace;
        white-space: pre-wrap;
        word-wrap: break-word;
        margin-bottom: 30px;
    }

    /* Button Style */
    .card button {
        background-color: #ff4d4d;
        color: white;
        border: none;
        padding: 10px 20px;
        font-size: 1.2em;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s ease;
    }

    /* Button Hover Effect */
    .card button:hover {
        background-color: #ff3333;  /* Darker red on hover */
    }

    /* Media query for responsive design */
    @media (max-width: 500px) {
        .card {
            width: 90%;
        }

        .card h1 {
            font-size: 2em;
        }

        .card p {
            font-size: 1em;
        }

        .card button {
            font-size: 1em;
        }
    }
    </style>
</head>
<body>
    <div class="container">
        <div class="card">
            <h1>400 - Invalid Credentials</h1>
            <p>The username or password you entered is incorrect. Please check your credentials and try again.</p>
            <p><strong>Error Details:</strong></p>
            <pre class="error-details">Error: Invalid login attempt. The credentials do not match our records.</pre>
            <button onclick="window.location.href = '/login';">Back to Login</button>
        </div>
    </div>
</body>
</html>
